{% extends 'base.html.twig' %}

{% block title %}Autolog{% endblock %}

{% block body %}

    <div class="content">
        <h1>⏱ Flex</h1>
        <table class="flex">
            {% for week, duration in weekly_duration %}
                <tr>
                    <td>
                        {{ week }}
                    </td>
                    <td style="text-align: right; padding-left: 3rem;">
                        {{ duration }}
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}
